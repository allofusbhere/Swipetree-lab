// rc2 config — leave NETLIFY_BASE empty for local-only or when site is hosted on Netlify itself.
window.SWIPE_CONFIG = {
  // If your app is on GitHub Pages and your function is on Netlify, set this to your Netlify site URL.
  // Example: "https://family-tree-swipe.netlify.app"
  NETLIFY_BASE: ""
};
